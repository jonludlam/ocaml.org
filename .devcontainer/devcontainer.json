{
  "name": "OCaml",
  "dockerFile": "Dockerfile",

  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },

  "extensions": [],

  "forwardPorts": [8080],

  "postCreateCommand": "unset DUNE_BUILD_DIR && make deps; DUNE_BUILD_DIR=/tmp/_build dune build",

  "remoteUser": "opam"
}

